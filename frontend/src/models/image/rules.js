const mimeTypes = [
    'image/jpeg',
    'image/png'
];

const fileExts = [
    'jpg',
    'jpeg',
    'png'
];

const maxBytes = 10 * 1024 * 1024;

export {
    mimeTypes,
    fileExts,
    maxBytes
}
